# Secretos y credenciales - NUNCA subir
.env
.env.*
*.pem
*.key
credentials*.json
ftp-credentials*
*password*

# Archivos temporales del sistema
Thumbs.db
.DS_Store

# Node (por si en el futuro se agregan herramientas build)
node_modules/

# Python (bot de automatizacion)
venv/
__pycache__/
*.pyc
logs/

# Imagenes OG generadas por el bot (se suben por FTP; se versionan solo cuando el bot las publica)
# assets/img/blog/*.jpg
logs/

# Seccion programmatic SEO desplegada directo al hosting, fuera de este repo
guias/
